import { useAuth } from "../lib/auth"
export default function AdminDashboardPage(){const {user,signOut}=useAuth();return <main className="role-dashboard"><header><div><p>Admin / HR workspace</p><h1>Welcome, {user?.displayName}</h1></div><button onClick={signOut}>Sign out</button></header><section><h2>Secure administration</h2><p>Organization metrics appear here after attendance, leave, payroll, and employee services are connected.</p></section><nav aria-label="Admin navigation">{["Employees","Attendance","Leave and Approvals","Teams and Tasks","Payroll","Documents","Reports and Analytics","Security and Audit Logs","Settings"].map(x=><a href="#" aria-disabled="true" key={x}>{x}<small>Service not connected</small></a>)}</nav></main>}
