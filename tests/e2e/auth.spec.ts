import { expect,test } from "@playwright/test"
test("unified login is role-neutral and accessible",async({page})=>{await page.route("**/api/auth/refresh",r=>r.fulfill({status:401,contentType:"application/json",body:'{"message":"Session expired."}'}));await page.goto("/sign-in");await expect(page.getByRole("heading",{name:"Welcome back"})).toBeVisible();await expect(page.getByLabel("Company email or Employee ID")).toBeVisible();await expect(page.getByRole("button",{name:"Sign In"})).toBeVisible();await expect(page.getByText(/choose admin|choose employee/i)).toHaveCount(0);await page.getByRole("button",{name:"Show password"}).click();await expect(page.getByLabel("Password",{exact:true})).toHaveAttribute("type","text")})
test("forgot-password confirmation does not enumerate accounts",async({page})=>{await page.route("**/api/auth/password/forgot",r=>r.fulfill({status:202,contentType:"application/json",body:'{"message":"accepted"}'}));await page.goto("/forgot-password");await page.getByLabel("Company email or Employee ID").fill("unknown@example.com");await page.getByRole("button",{name:"Send recovery instructions"}).click();await expect(page.getByText("If the account can be verified, recovery instructions will be sent.")).toBeVisible()})
